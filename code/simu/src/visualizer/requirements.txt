# Cache-Aware vs WFD算法性能可视化程序依赖包
# 
# 安装方法:
# pip install -r requirements.txt

# 核心数据处理和可视化库
pandas>=1.3.0          # 数据处理和分析
matplotlib>=3.3.0       # 基础绘图库
numpy>=1.20.0          # 数值计算库

# 增强可视化库 (可选，用于完整版可视化)
seaborn>=0.11.0        # 统计图表库，基于matplotlib

# 如果遇到中文显示问题，可以安装中文字体包
# 对于Windows系统，通常已经包含SimHei字体
# 对于Linux/Mac系统，可能需要额外安装中文字体

# 最小依赖版本 (simple_visualize_results.py):
# matplotlib>=3.3.0
